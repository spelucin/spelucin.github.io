[build]
  command = "hugo mod get -u && hugo --gc --minify -b $URL --disableFastRender"
  publish = "public"

[build.environment]
  NODE_ENV = "production"
  GO_VERSION = "1.20"
  TZ = "UTC"  # Set to preferred timezone

[context.production.environment]
  HUGO_VERSION = "0.112.7"
  HUGO_ENV = "production"

[context.deploy-preview.environment]
  HUGO_VERSION = "0.112.7"